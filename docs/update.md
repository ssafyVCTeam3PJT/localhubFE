0. 서울_레포츠.json 공공데이터를 sqlite를 통해 디비에 저장한다.
1. api-spec.md 에 저장된 api들을 FastAPI로 구현한다.
  -> 구현하기 전에 공공 데이터와 api 스펙을 보고 디비를 설계한 후에 md 파일로 설계 구조를 작성한다 (테이블 설계, ERD 등등)
  -> 실제 설계대로 sqlite에 스키마와 테이블을 구축한다.
  -> api를 우선순위대로 구현한다.
  -> 각 api는 범위별로 구현할 때마다 테스트할 수 있도록한다. (요청 날려서 응답 제대로 오는지 확인.)
